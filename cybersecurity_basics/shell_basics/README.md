TASK-0: the script changing the user work directory to /home/user directory, we can also use cd ~ which is same as cd
TASK-1: the script lists all files in long format from the current directory, the total number of files and then each line has diferent parts, it starts with file permission, number of hard links, file owner, group, size in bytes, creation/modification date and lastly file name.
